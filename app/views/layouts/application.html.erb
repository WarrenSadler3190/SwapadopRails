<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  <div id="container">
     <div class="contain-to-grid sticky">
      <nav class="top-bar" data-topbar>
        <ul class="title-area">
          <li class="name">
            <h1><a href="/"><img id="logo" src="<%= asset_path('swapadopLogo.png')%>" alt=""></a></h1>
          </li>
          <li class="toggle-topbar menu-icon">Menu</li>
        </ul>
        <section class="top-bar-section">
          <ul class="right">
            <% if user_signed_in? %>
              <li class="has-dropdown">
                <% if current_user.image %>
                  <a href="#">Hello <%= current_user.name %> <img src="<%=current_user.image%>" class="user-image" alt=""></a>
                <% else %>
                  <a href="#"><%=fa_icon "heart" %> <%= current_user.email %></a>
                <% end %>
                <ul class="dropdown">
                  <li><%=link_to 'My Items', items_path %></li>
                  <li><%=link_to 'Upload Item', new_item_path %></li>
                  <li><%=link_to 'Edit profile', edit_user_registration_path %></li>
                  <li><%=link_to 'Logout', destroy_user_session_path, method: :delete%></li>
                </ul>
              </li>
            <% else %>
              <li><%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook) %></li>
              <li><%=link_to "Sign up", new_user_registration_path %></li>
              <li><%=link_to "Login", new_user_session_path %></li>
            <% end %>   
            <li><%=link_to "Swapadopolis", swapadopolis_index_path%></li>
            <li class="has-form">
                <div class="row collapse">
                  <%= form_tag swapadopolis_index_path, :method => :get do%>
                  <div class="large-8 small-9 columns">
                    <%= text_field_tag :search, params[:search], :placeholder=> "Find Stuff"%>
                  </div>
                  <div class="large-4 small-3 columns">
                    <%= submit_tag "Search", :name => nil ,:class=>"success button expand"%>
                  </div>
                  <% end %>
                </div>
            </li>         
          </ul>
        </section>
      </nav>
    </div>
    <% if notice %>
      <div data-alert class="alert-box">
        <%= notice %>
        <a href="#" class="close">&times;</a>
      </div>
    <% elsif alert %>
      <div data-alert class="alert-box warning">
        <%= alert %>
        <a href="#" class="close">&times;</a>
      </div>
    <% end %>
    <%= yield %>

    <%= javascript_include_tag "application" %>
  </div>
  </body>
</html>